# РХАХТА РХА-Н тавилын карт

Энэхүү веб аппликейшн нь Google Sheets-ээс өгөгдөл уншиж, HTML хэлбэрээр харах, тоноглолын нэрээр шүүх, мөн "өөрчлөлтүүд" баганын утгыг засах боломжтой. Зассан утгуудыг Google Sheets дээр шууд хадгалах боломжтой.

## ⚡ Хурдан эхлэх

1. **`start-server.bat` файл дээр давхар дарах** (Windows)
2. **Browser дээр `http://localhost:8000` нээх**
3. **Готово!**

## 🌐 Өөр компьютерээс хандах заавар

### Арга 1: Локал сүлжээнд байрлуулах (Ижил WiFi/Network)

#### Windows дээр:

1. **Firewall тохируулах:**
   - Windows Settings → Network & Internet → Windows Defender Firewall
   - "Allow an app through firewall" → "Change settings"
   - "Python" эсвэл "Python 3.x" олох → Private болон Public checkbox-уудыг сонгох
   - Хэрэв олдохгүй бол "Allow another app" → Python-ийн path оруулах

2. **Компьютерийн IP хаягийг олох:**
   - Command Prompt нээх (`Win + R` → `cmd`)
   - Дараах командыг бичих:
     ```bash
     ipconfig
     ```
   - "IPv4 Address" олох (жишээ: `192.168.1.100`)

3. **Сервер ажиллуулах (бүх интерфейс дээр):**
   - Command Prompt дээр:
     ```bash
     cd "C:\Users\User\Desktop\tawiliin card tusul"
     python -m http.server 8000 --bind 0.0.0.0
     ```
   - Эсвэл `start-server.bat` файлыг засах:
     ```batch
     @echo off
     echo Starting Python HTTP server on port 8000...
     echo Open your browser to http://localhost:8000
     echo Other computers can access: http://YOUR_IP:8000
     cd /d "%~dp0"
     python -m http.server 8000 --bind 0.0.0.0
     pause
     ```

4. **Өөр компьютерээс хандах:**
   - Өөр компьютерийн browser дээр дараах хаяг нээх:
     ```
     http://YOUR_IP:8000
     ```
   - Жишээ: `http://192.168.1.100:8000`

#### Mac/Linux дээр:

1. **Terminal нээх**

2. **IP хаягийг олох:**
   ```bash
   ifconfig
   # эсвэл
   ip addr
   ```
   - "inet" эсвэл "inet addr" олох (жишээ: `192.168.1.100`)

3. **Сервер ажиллуулах:**
   ```bash
   cd "/path/to/tawiliin card tusul"
   python3 -m http.server 8000 --bind 0.0.0.0
   ```

4. **Өөр компьютерээс хандах:**
   - `http://YOUR_IP:8000`

### Арга 2: Онлайн веб дээр байрлуулах (Интернэтээр хандах)

#### GitHub Pages ашиглах (Үнэгүй):

**Алхам 1: GitHub account үүсгэх**

1. **GitHub website руу орох:**
   - https://github.com дээр орох
   - "Sign up" товчийг дарах
   - Username, email, password оруулах
   - "Create account" дарах
   - Email баталгаажуулах

**Алхам 2: Repository үүсгэх**

1. **GitHub дээр нэвтрэх**

2. **Repository үүсгэх:**
   - Баруун дээд буланд "+" товч → "New repository" дарах
   - **Repository name:** `tawiliin-card` (эсвэл өөр нэр)
   - **Description:** "РХАХТА РХА-Н тавилын карт" (сонголттой)
   - **Visibility:** "Public" сонгох (GitHub Pages үнэгүй зөвхөн Public repository-д ажиллана)
   - "Add a README file" checkbox-ийг сонгохгүй (бид өөрсдөө файлууд upload хийх)
   - "Create repository" дарах

**Алхам 3: Файлуудыг upload хийх**

1. **Repository дээр орох**

2. **Файлууд upload хийх:**
   - "uploading an existing file" товчийг дарах
   - Эсвэл "Add file" → "Upload files" дарах
   - Бүх файлуудыг drag & drop хийх эсвэл "choose your files" дарах:
     - ✅ `index.html` (заавал)
     - ✅ `BUYANTUHAA.pdf` (PDF файлууд - заавал)
     - ✅ `README.md` (сонголттой)
     - ✅ Бусад PDF файлууд (хэрэв байвал)

3. **Commit хийх:**
   - Доод талд "Commit changes" хэсэгт:
     - **Commit message:** "Initial commit" (эсвэл өөр нэр)
     - "Commit changes" товчийг дарах

**Алхам 4: GitHub Pages идэвхжүүлэх**

1. **Repository → Settings руу орох:**
   - Repository дээр "Settings" tab дарах

2. **Pages тохируулах:**
   - Зүүн талын menu-ээс "Pages" сонгох
   - **Source:**
     - "Deploy from a branch" сонгох
   - **Branch:**
     - Branch: "main" (эсвэл "master" - repository-ийн гол branch)
     - Folder: "/ (root)" сонгох
   - "Save" товчийг дарах

3. **Хаяг авах:**
   - Хэдэн минут хүлээх (GitHub Pages deploy хийхэд)
   - Settings → Pages дээр дахин орох
   - "Your site is live at" хэсэгт хаяг харагдана
   - Жишээ: `https://yourusername.github.io/tawiliin-card/`
   - ⚠️ **Энэ хаягийг хуулж авах хэрэгтэй!**

**Алхам 5: Google Cloud Console дээр тохируулах**

1. **Google Cloud Console руу орох:**
   - https://console.cloud.google.com/
   - Project сонгох

2. **OAuth 2.0 Client ID засах:**
   - "APIs & Services" → "Credentials" руу орох
   - OAuth 2.0 Client ID дээр дарах (эсвэл шинээр үүсгэх)

3. **Authorized JavaScript origins нэмэх:**
   - "Authorized JavaScript origins" хэсэгт "+ ADD URI" дарах
   - GitHub Pages хаягаа оруулах:
     ```
     https://yourusername.github.io
     ```
   - ⚠️ **HTTPS ашиглах хэрэгтэй!** (http:// биш)
   - "Save" дарах

4. **Authorized redirect URIs нэмэх:**
   - "Authorized redirect URIs" хэсэгт "+ ADD URI" дарах
   - GitHub Pages хаягаа оруулах:
     ```
     https://yourusername.github.io
     ```
   - "Save" дарах

5. **OAuth Consent Screen шалгах:**
   - "APIs & Services" → "OAuth consent screen" руу орох
   - "Test users" хэсэгт бүх хэрэглэгчдийн email нэмэх
   - Хэрэв production дээр байрлуулах бол "Publish" хийх

**Алхам 6: Application дээр тохируулах**

1. **GitHub Pages хаягаар нээх:**
   - Browser дээр GitHub Pages хаягаа нээх
   - Жишээ: `https://yourusername.github.io/tawiliin-card/`

2. **Нэвтрэх:**
   - Нэвтрэх нэр: `mb` эсвэл `op`
   - Нууц үг: `123`

3. **Google OAuth 2.0 Client ID оруулах:**
   - "Google OAuth 2.0 Client ID" input field-д Client ID оруулах
   - Жишээ: `298828856664-7phl22m56lopgbga190r6f2rjthvidc2.apps.googleusercontent.com`

4. **Google Sheets URL оруулах:**
   - "Дэд станц" dropdown-оос дэд станц сонгох
   - "📥 Google Sheets унших" товчийг дарах

5. **Хадгалах:**
   - "Тавил оруулах" баганад утга оруулах
   - "☁️ Google Sheets дээр хадгалах" товчийг дарах
   - Google account-аар нэвтрэх хүсэлт гарна
   - Зөвшөөрөх

**Алхам 7: PDF файлууд нэмэх (хэрэв байвал)**

1. **Repository дээр орох**

2. **PDF файлууд upload хийх:**
   - "Add file" → "Upload files" дарах
   - PDF файлуудыг drag & drop хийх
   - "Commit changes" дарах

3. **PDF файлуудын нэрийг шалгах:**
   - `index.html` файл дээр `stationPdfMap` object-д PDF файлын нэрийг зөв оруулсан эсэхийг шалгах
   - Жишээ:
     ```javascript
     const stationPdfMap = {
       "Буянт Ухаа": "BUYANTUHAA.pdf"
     };
     ```

**⚠️ ЧУХАЛ: GitHub Pages тохируулах**

- ⚠️ **Repository нь Public байх ёстой** (GitHub Pages үнэгүй зөвхөн Public repository-д ажиллана)
- ⚠️ **HTTPS ашиглах:** Google OAuth 2.0 HTTPS шаардлагатай
- ⚠️ **Deploy хийхэд хэдэн минут хүлээх:** GitHub Pages deploy хийхэд 1-5 минут зарцуулна
- ⚠️ **PDF файлууд:** PDF файлуудыг мөн repository-д upload хийх хэрэгтэй
- ⚠️ **Файлын нэр:** Файлын нэрийг зөв оруулах (том жижиг үсэг ялгаатай)

#### Netlify ашиглах (Үнэгүй):

1. **Netlify account үүсгэх:**
   - https://netlify.com дээр бүртгүүлэх

2. **Сайт байрлуулах:**
   - "Add new site" → "Deploy manually"
   - Бүх файлуудыг drag & drop хийх
   - Автоматаар URL өгөгдөнө (жишээ: `https://random-name-123.netlify.app`)

3. **Google Cloud Console дээр тохируулах:**
   - OAuth 2.0 Client ID → Authorized JavaScript origins:
     - Netlify URL нэмэх (жишээ: `https://random-name-123.netlify.app`)
   - Authorized redirect URIs:
     - Netlify URL нэмэх

#### Vercel ашиглах (Үнэгүй):

1. **Vercel account үүсгэх:**
   - https://vercel.com дээр бүртгүүлэх

2. **GitHub repository холбох:**
   - "New Project" → GitHub repository сонгох
   - Автоматаар deploy хийгдэнэ

3. **Google Cloud Console дээр тохируулах:**
   - OAuth 2.0 Client ID → Authorized JavaScript origins:
     - Vercel URL нэмэх
   - Authorized redirect URIs:
     - Vercel URL нэмэх

### ⚠️ ЧУХАЛ: Google Cloud Console тохируулах

Өөр компьютер эсвэл онлайн веб дээр байрлуулах үед:

1. **Google Cloud Console руу орох:**
   - https://console.cloud.google.com/

2. **OAuth 2.0 Client ID засах:**
   - "APIs & Services" → "Credentials"
   - OAuth 2.0 Client ID дээр дарах
   - **Authorized JavaScript origins:**
     - Локал сүлжээ: `http://YOUR_IP:8000` нэмэх
     - Онлайн веб: `https://yourdomain.com` нэмэх
   - **Authorized redirect URIs:**
     - Локал сүлжээ: `http://YOUR_IP:8000` нэмэх
     - Онлайн веб: `https://yourdomain.com` нэмэх
   - "Save" дарах

3. **OAuth Consent Screen:**
   - "Test users" хэсэгт бүх хэрэглэгчдийн email нэмэх
   - Хэрэв production дээр байрлуулах бол "Publish" хийх

### 🔐 Нэвтрэх мэдээлэл

- **Админ user:**
  - Нэвтрэх нэр: `mb`
  - Нууц үг: `123`

- **Оператор user:**
  - Нэвтрэх нэр: `op`
  - Нууц үг: `123`

### 📝 Анхаарах зүйлс

- ⚠️ **Firewall тохируулах:** Локал сүлжээнд байрлуулах үед firewall дээр port 8000-ийг зөвшөөрөх хэрэгтэй
- ⚠️ **IP хаяг өөрчлөгдөж болно:** WiFi сүлжээ өөрчлөгдвөл IP хаяг өөрчлөгдөж болно
- ⚠️ **HTTPS шаардлагатай:** Онлайн веб дээр байрлуулах үед HTTPS ашиглах нь зөв (Google OAuth 2.0)
- ⚠️ **PDF файлууд:** PDF файлуудыг мөн upload хийх хэрэгтэй (GitHub Pages, Netlify, Vercel)

## Хэрхэн ажиллуулах

### ⚠️ ЧУХАЛ: Локал серверээр ажиллуулах (заавал)

HTML файлыг шууд нээхэд (file://) Google OAuth 2.0 ажиллахгүй. Тиймээс локал сервер ашиглах шаардлагатай.

#### Арга 1: Python ашиглах (Хамгийн хялбар)

**Хялбар арга: Script файл ашиглах**

1. **`start-server.bat` файл дээр давхар дарах** (Windows)
   - Эсвэл PowerShell дээр: `.\start-server.ps1`
   - Автоматаар сервер ажиллана

**Гараар ажиллуулах:**

1. **Terminal/Command Prompt нээх:**
   - Windows: `Win + R` → `cmd` → Enter
   - Эсвэл VS Code дээр Terminal нээх (`Ctrl + ~`)

2. **Энэ folder руу орох:**
   ```bash
   cd "C:\Users\User\Desktop\tawiliin card tusul"
   ```

3. **Python сервер ажиллуулах:**
   ```bash
   python -m http.server 8000
   ```
   
   Хэрэв `python` ажиллахгүй бол `python3` эсвэл `py` туршиж үзэх:
   ```bash
   python3 -m http.server 8000
   # эсвэл
   py -m http.server 8000
   ```

4. **Браузер дээр нээх:**
   - Browser дээр дараах хаяг нээх: `http://localhost:8000`
   - Эсвэл: `http://127.0.0.1:8000`

#### Арга 2: Node.js ашиглах

1. **Terminal/Command Prompt нээх**

2. **http-server суулгах (нэг удаа):**
   ```bash
   npm install -g http-server
   ```

3. **Энэ folder руу орох:**
   ```bash
   cd "C:\Users\User\Desktop\tawiliin card tusul"
   ```

4. **Сервер ажиллуулах:**
   ```bash
   http-server -p 8000
   ```

5. **Браузер дээр нээх:**
   - `http://localhost:8000`

#### Арга 3: VS Code Live Server (Хамгийн хялбар)

1. **VS Code дээр "Live Server" extension суулгах**
2. **`index.html` файл дээр баруун товч дарах**
3. **"Open with Live Server" сонгох**
4. **Автоматаар browser дээр нээгдэнэ**

## Google Sheets хадгалах тохиргоо

### OAuth 2.0 Client ID үүсгэх

1. **Google Cloud Console руу орох:**
   - https://console.cloud.google.com/

2. **Шинэ project үүсгэх:**
   - "Select a project" → "New Project"
   - Project нэр оруулах → "Create"

3. **Google Sheets API идэвхжүүлэх:**
   - "APIs & Services" → "Library"
   - "Google Sheets API" хайх → "Enable"

4. **OAuth 2.0 Client ID үүсгэх:**
   - "APIs & Services" → "Credentials"
   - "Create Credentials" → "OAuth client ID"
   - Application type: "Web application"
   - Name: "Тавилын карт"
   - **Authorized JavaScript origins:**
     - `http://localhost`
     - `http://127.0.0.1`
     - (Production дээр байрлуулах бол: `https://yourdomain.com`)
   - **Authorized redirect URIs:**
     - `http://localhost`
     - `http://127.0.0.1`
     - (Production дээр байрлуулах бол: `https://yourdomain.com`)
   - "Create" дарах

5. **Client ID хуулж авах:**
   - Client ID нь: `298828856664-7phl22m56lopgbga190r6f2rjthvidc2.apps.googleusercontent.com`
   - Энэ нь аль хэдийн application дээр оруулсан байна

### OAuth Consent Screen тохируулах

1. **OAuth Consent Screen тохируулах:**
   - "APIs & Services" → "OAuth consent screen"
   - User Type: "External" сонгох → "Create"
   - **App information:**
     - App name: "РХАХТА РХА-Н тавилын карт"
     - User support email: Таны email
     - Developer contact information: Таны email
   - "Save and Continue"

2. **Scopes нэмэх:**
   - "Add or Remove Scopes"
   - "https://www.googleapis.com/auth/spreadsheets" нэмэх
   - "Update" → "Save and Continue"

3. **Test users нэмэх (ЧУХАЛ!):**
   - "Test users" → "Add users"
   - Таны Google account email оруулах (жишээ: `sarantuyamunhbayar@gmail.com`)
   - "Add" дарах
   - "Save and Continue"

4. **Summary:**
   - Бүх мэдээллийг шалгах
   - "Back to Dashboard" дарах

**⚠️ ЧУХАЛ:** Хэрэв "Access blocked" алдаа гарвал:
- OAuth consent screen дээр "Test users" хэсэгт таны email нэмсэн эсэхийг шалгах
- Email зөв оруулсан эсэхийг шалгах
- Хэрэв нэмээгүй бол дахин нэмэх

**⚠️ ЧУХАЛ:** Хэрэв "403 Forbidden" алдаа гарвал (хүснэгтийг өөрчилж болохгүй):
- Google Sheets нээх
- **Share** товчийг дарах (баруун дээд буланд)
- "Anyone with the link" → **"Editor"** сонгох (бичих эрх шаардлагатай)
- ⚠️ **"Viewer" биш, "Editor" сонгох хэрэгтэй**
- "Done" дарах
- Application дээр дахин оролдох

## Хэрхэн ашиглах

1. **Localhost дээр ажиллуулах** (дээрх аргуудын аль нэгийг ашиглах)

2. **Google Sheets URL оруулах:**
   - Google Sheets нээх
   - **Share** товчийг дарах (баруун дээд буланд)
   - "Anyone with the link" → **"Editor"** сонгох (бичих эрх шаардлагатай)
   - ⚠️ **"Viewer" биш, "Editor" сонгох хэрэгтэй** (хэрэв "Editor" сонгохгүй бол хадгалах боломжгүй)
   - "Done" дарах
   - URL-ийг хуулж application дээр оруулах

3. **"📥 Google Sheets унших" товчийг дарах**

4. **Sheet сонгох** (хэрэв олон sheet байвал)

5. **Тоноглолын нэр сонгох** (хэрэв шүүх бол)

6. **"өөрчлөлтүүд" баганын нүднүүдэд утга оруулах**

7. **"☁️ Google Sheets дээр хадгалах" товчийг дарах:**
   - Google account-аар нэвтрэх хүсэлт гарна
   - Зөвшөөрөх
   - Утгууд Google Sheets дээр хадгалагдана

### Онлайн веб дээр байрлуулах

> **📖 Дэлгэрэнгүй заавар:** Дээрх "Өөр компьютерээс хандах заавар" хэсэгт GitHub Pages ашиглах дэлгэрэнгүй зааврыг үзнэ үү.

#### Netlify ашиглах
1. https://netlify.com дээр бүртгүүлэх
2. "Add new site" → "Deploy manually"
3. HTML файл болон Excel файлыг drag & drop хийх
4. Автоматаар URL өгөгдөнө

#### Vercel ашиглах
1. https://vercel.com дээр бүртгүүлэх
2. GitHub repository холбох эсвэл файлуудыг upload хийх
3. Автоматаар deploy хийгдэнэ

## Онцлог

- ✅ Google Sheets-ээс өгөгдөл унших
- ✅ Sheet (хуудас) сонгох
- ✅ Тоноглолын нэрээр шүүх
- ✅ Эхний 3 багана (Д/д, Тоноглолын нэр, Хамгаалалтын төрөл) merge хийх
- ✅ "өөрчлөлтүүд" баганын нүднүүдэд утга оруулах
- ✅ Зассан утгуудыг Google Sheets дээр шууд хадгалах
- ✅ Excel файл татаж авах боломж

## Браузер дэмжлэг

- **Chrome/Edge**: Бүх функц ажиллана
- **Firefox/Safari**: Google OAuth 2.0 ажиллана, гэхдээ зарим функц хязгаарлагдмал байж болно

## Ашигласан технологи

- **SheetJS (XLSX.js)** - CSV/Excel файл унших/бичих
- **Google Sheets API v4** - Google Sheets дээр хадгалах
- **Google Identity Services** - OAuth 2.0 authentication
- **Vanilla JavaScript** - Ямар ч framework шаардлагагүй

## Анхаар

- ⚠️ **Localhost дээр ажиллуулах шаардлагатай** (file:// protocol ажиллахгүй)
- ⚠️ **Google OAuth 2.0 Client ID шаардлагатай** (Google Sheets дээр хадгалахад)
- ⚠️ **Google Sheets нь "Anyone with the link can view" байх ёстой** (уншихын тулд)
- ⚠️ **Google Sheets API идэвхжүүлсэн байх ёстой** (Google Cloud Console дээр)

